import Vue from 'vue'
import Vuex from 'vuex'
import projectData from '@/data/data.json'
Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    projectData
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
